/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    defaultLocale: 'en-US',
    localeDetection: false,
    locales: ['en-US'],
  },
  reactStrictMode: true,
}

module.exports = nextConfig
